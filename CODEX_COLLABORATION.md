# Codex 主会话与子会话协作说明

本文档用于在家里电脑和公司电脑之间重建相同的 Codex 协作方式。Git 负责同步代码、规则和交接信息；Codex 会话树本身不通过 Git 同步。

## 一、协作结构

```text
用户
  ↓
主会话（统一调度、整合、验收、交付）
  ├─ S01 汉字子会话
  ├─ S02 炸弹迷宫子会话
  ├─ S04 主题学习与总复习子会话
  ├─ S05 音标子会话
  └─ T02 图片生成子会话
```

用户原则上只需要向主会话提出需求。主会话决定是否拆分子任务、交给谁、能否并行，以及最后如何整合。

## 二、主会话职责

主会话是项目唯一的整合入口，负责：

- 接收用户需求并确认本轮范围。
- 读取 `AGENTS.md` 和 `PROJECT_HANDOFF.md`。
- 检查 Git 工作区，保护用户已有修改。
- 判断任务属于汉字、炸弹、主题、音标或图片生成模块。
- 把可独立完成的工作拆成边界清楚的子任务。
- 避免两个子会话同时修改同一文件。
- 管理共享文件，例如导航、`package.json`、README 和跨模块测试。
- 接收子会话的实现结果和测试证据。
- 处理子会话无法完成的浏览器接力或跨模块验证。
- 运行最终静态检查、Node 测试和必要的 Microsoft Edge 验收。
- 汇总修改文件、行为变化、测试结果和未解决问题。
- 只有在用户明确授权后才提交或推送 Git。

主会话不能把“某个子会话说已完成”直接等同于项目已完成。最终交付前仍需检查跨模块回归、资源版本和工作区状态。

## 三、各子会话职责

### S01 汉字

主要范围：`index.html`、`styles.css`、汉字相关的 `src/`、`data/`、Node 与 Edge 测试。

固定规则：

- 普通复习每天最多20个。
- 不采用音标模块的全量复习规则。
- 保持汉字存储和旧数据迁移边界。

### S02 炸弹迷宫

主要范围：`bomb-game.html`、`bomb-game.css`、`bomb-game.js`、炸弹测试和截图。

固定规则：

- 每关必须有5个目标实例。
- 合格字不足5个时，可以把同一个字作为不同目标实例重复出现。
- 炸弹进度使用独立存储。
- 交互元素获得焦点时，不能被全局 Enter/Space 快捷键劫持。

### S04 主题学习与总复习

主要范围：主题页、总复习页、`src/theme-overview.js`、`assets/themes/` 和相关测试。

固定规则：

- 总复习是与汉字、主题学习、音标平行的顶层模块。
- 总词库只记录真正学习过的主题单词。
- 总复习覆盖全部已学词。
- 单词配图必须逐词对应。

### S05 音标

主要范围：`phonetics.html`、`phonetics.css`、音标数据、`src/phonetics-*` 和相关测试。

固定规则：

- 音标数据固定为48项。
- 每次复习包含全部非 `mastered` 项，数量为0–48。
- 点星后立即从普通复习和临时复习中排除。
- 音标使用独立存储，不能修改汉字的固定20规则。

### T02 图片生成

主要范围：生成图片、检查尺寸与内容、把最终资源保存到 `assets/`。

固定规则：

- 中间稿保存在被 Git 忽略的 `output/`。
- 未明确授权时，不修改网页业务代码。
- 不泄露 API Key。
- 图片必须经过人工预览和页面实际显示检查。

## 四、标准协作流程

```text
1. 用户提出需求
2. 主会话读取项目规则并检查 Git 状态
3. 主会话划定范围和禁止修改项
4. 主会话判断顺序执行或并行执行
5. 子会话实现并完成模块内测试
6. 子会话回传文件、结果、证据和问题
7. 主会话检查冲突并整合
8. 主会话运行跨模块和浏览器回归
9. 主会话向用户交付
10. 用户明确同意后，主会话提交并推送 Git
11. 更新 PROJECT_HANDOFF.md，供另一台电脑继续
```

## 五、主会话下发任务模板

```text
负责模块：<S01/S02/S04/S05/T02>

目标：
<本轮需要达到的具体结果>

允许修改：
- <文件或目录>

禁止修改：
- <其他模块或共享业务>

必须保持：
- <存储规则>
- <复习规则>
- <交互或视觉规则>

验收要求：
- 静态检查
- 相关 Node 测试
- 桌面与390px
- 必要的真实 Edge 验收
- 资源 HTTP 200

完成后回传：
- 实现结果
- 修改文件
- 测试结果
- 存储影响
- 未修改范围
- 尚未解决问题
```

## 六、子会话回传模板

```text
模块：
本轮目标：

实现结果：
- ...

修改文件：
- ...

验证结果：
- 静态检查：
- Node：
- Edge：
- 390px：
- HTTP：

存储影响：
- ...

未修改范围：
- ...

尚未解决问题：
- 无 / 具体问题
```

## 七、并行和冲突规则

- 不修改相同文件的任务可以并行。
- 会修改相同文件的任务必须顺序执行，或使用独立 Git worktree。
- 导航、`package.json`、README 和共享测试属于高冲突文件，由主会话统一协调。
- 子会话发现范围外问题时，只回报，不擅自扩大修改范围。
- 子会话的修改在共享工作区中可能立即可见，不得覆盖来源不明的修改。
- 主会话整合前先运行 `git diff`，确认每项变化都有明确来源。

## 八、内部子代理与手动独立聊天

内部子代理由主会话创建，结果自动回传主会话，适合边界清楚、可以独立完成的工作。

手动独立聊天由用户在同一项目中建立，不会自动形成父子关系，需要通过 Git、`PROJECT_HANDOFF.md` 或用户转述进行交接。多个聊天同时写代码时，建议使用不同 worktree。

## 九、跨电脑继续方式

家里电脑结束工作前：

```powershell
git status --short --branch
pnpm run check
pnpm test
git add -A
git commit -m "<本轮说明>"
git push origin main
```

公司电脑开始工作时：

```powershell
git status --short --branch
git pull origin main
pnpm install
pnpm run check
pnpm test
```

新主会话启动词：

```text
请完整读取 AGENTS.md、PROJECT_HANDOFF.md 和 CODEX_COLLABORATION.md，检查 git status，然后从当前 main 分支继续。你是本项目的主会话，负责模块分工、冲突控制、整合和最终验收。未经我明确授权，不要提交或推送 Git。
```

## 十、完成定义

只有同时满足以下条件，主会话才能报告本轮完成：

- 用户要求的行为已真实实现。
- 没有超出授权范围修改其他模块。
- `pnpm run check` 和 `pnpm test` 通过。
- UI 改动完成目标浏览器、桌面和390px验收。
- 所有实际引用资源返回 HTTP 200。
- 存储边界和既有复习规则没有回退。
- 工作区差异经过审阅。
- 未解决问题已明确列出。
- 如用户要求 Git 提交，远端和本地分支已核对同步。

参考：[OpenAI Projects and chats](https://developers.openai.com/codex/projects)
