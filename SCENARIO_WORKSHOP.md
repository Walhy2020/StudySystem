# 情景配图与新单词

## 最新制作方式（2026-09-12）

用户直接在会话里提供一个情景的几句对话。由助手使用 **imagegen skill 的内置 image_gen** 生成正式插画，检查图片后接入网站，并整理英文、英式音标、中文及逐词释义。

网页只负责学习，不再提供创建表单、在线生成入口或 OpenAI API 后端；无需在网页配置 API Key。旧版网页生成代码和对应测试已移除，已有学习记录保持不变。忽略目录 .local-scenarios/ 仍受保护，不删除其中任何私人文件。

## 当前配图

- 情景：第一次见面（Mia / Leo）。
- 文件：assets/scenarios/first-meeting-v1.png。
- 内置 image_gen 生成，1254×1254，RGB PNG。
- 原正式插画保留为情景封面。学习页使用独立 Mia / Leo 透明人物图层。
- `mia-sprite-v1.png` 为 560×1080 RGBA，`leo-sprite-v1.png` 为 521×1080 RGBA；人物由内置 image_gen 生成，再按用户授权以本地 rembg/u2netp 去除假透明背景。处理过程不上传图片，网页无需抠图工具或模型。
- 旧代码绘制的几何人物已从页面移除。
- 配图与代码进入 Git，换电脑拉取后可直接使用，不需要重新生成。
- 完整生成提示词见 assets/scenarios/first-meeting-v1.prompt.md。

- 情景：这是什么？（What Is It?，Mia / Leo）。
- 文件：assets/scenarios/what-is-it-classroom-v1.png。
- 内置 image_gen 生成，1254×1254，RGB PNG；同时用于情景封面和学习舞台背景。
- 桌面中央严格呈现 1 支蓝色笔、3 支黄色铅笔、1 支红色马克笔、3 块绿色橡皮，不含文字、人物或额外相似文具。
- Mia / Leo 继续使用独立透明人物图层，缩小并固定在左右两侧，为中央文具保留清晰区域。
- 教材印刷第 6-7 页的故事已整理为 14 句连续对话，依次练习 `What is it? / It's ...`、`What are they? / They're ...`、颜色判断与红/绿橡皮纠正表达，配五道对应回应题。
- 提到文具时，黑板中央会显示带白色圆底的独立物品强调图；蓝色笔、三支黄色铅笔、红色马克笔、三块绿色橡皮和三块红色橡皮分别使用独立透明 PNG。
- 完整生成提示词见 assets/scenarios/what-is-it-classroom-v1.prompt.md。
- 独立文具强调图的生成记录见 assets/scenarios/focus-objects-v1.prompt.md。

## 分步对话播放（2026-09-11）

- 「从头重播」回到第一句并朗读，然后停住；用户点击 Next 才显示并朗读下一句，不自动推进。
- 「从头连播」始终从第一句开始，语音结束后自动依次播放当前情景的全部句子（第一次见面六句；这是什么十四句）。两种模式可随时切换，旧播放回调不会误推进。
- Mia 先出现，Leo 随第二句进入。桌面左侧为独立人物舞台，说话角色使用饱和橙金色双层轮廓光晕，收紧模糊范围以提高颜色辨识度，每 1 秒呼吸一次、仅 3 个周期（共 3 秒），随后停止呼吸并常亮至本句朗读结束；不再使用虚线框。只改变光晕强度，人物本身不闪烁；减少动态效果时保持常亮。
- 右侧仅保留当前一句聊天气泡，不累积历史对话；手机为上下布局。每个英文单词正下方显示它自己的英式音标，严格逐词对应。Previous / Next 可回看和朗读。
- 依赖浏览器语音的结束事件推进，不按猜测时长截断。无语音或失败时停止并提示，可用 Next 手动继续。
- 朗读期间显示暂停按钮，继续时重读当前句并保留逐句/连播模式；切换页面、练习、新单词时取消旧播放。
- 进入页面不自动出声；播放完成不会自动标记情景练习完成或把词标为学会。低动态偏好下关闭人物动画。

## 新单词学习记录

- 「认识对话 / 情景练习」旁的第三个「新单词」页签只收集当前情景中尚未学过的词，切换情景时同步切换词表。
- 按英文词形、小写去重；使用与总复习页面相同的统一总单词库，汇总当前浏览器中 Book1、主题学习和情景模式实际学过的词。
- 常见缩写如 I'm 拆为 i/am，排除角色专名，保留功能词。
- 提供英式音标、中文释义、来源句、手动朗读和「学会了」；支持显示已学词并重新学习。
- 情景对话完成不自动把所有词标为学会。
- 第一个情景有 16 个候选词，第二个情景有 19 个候选词；页面不再把两个情景的词汇汇总到一起，已存在于总单词库的词不会再显示为新词。
- 只写 mario-scenario-learning-v1.learnedWords，保留原情景完成和当前位置。
- 存储 schema 3 按情景独立保存对话行、认识对话/情景练习/新单词阶段和题号；旧版单情景顶层位置会迁移到当时活动情景。
- 不修改 Book1、主题或其他模块进度；总单词库通过只读聚合自动看到情景中明确学会的词。
- Git 不同步浏览器学习记录。

## 验证

```powershell
pnpm run check
pnpm test
node tests/scenario-browser-acceptance.mjs
node tests/scenario-workshop-browser.mjs
node tests/scenario-playback-browser.mjs
```

检查真实图片 HTTP 200、PNG 尺寸和透明像素、桌面/390px完整显示、人物依次出现、语音结束后推进、音标上下关系、暂停与重播、新单词持久化及存储隔离。浏览器语音测试使用受控回调验证调用和时序，不等同于人工听音。页面不发送生成 API 请求。
